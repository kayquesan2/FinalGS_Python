----------- CASO APRESENTE PROBLEMAS COM A EXECUÇÃO DOS SCRIPTS PELO DBEAVER, UTILIZE O ORACLE SQL DEVELOPER! ----------- 

INTEGRANTES: 

Kayque Ferreira dos Santos - RM552605
Marco Aurélio Morais Ennes - RM553201
André Alves da Silva - RM552639


SCRIPTS:

CREATE TABLE TB_PRODUCAO_PLASTICO (
    ID NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY PRIMARY KEY,
    ENTIDADE VARCHAR2(100),
    ANO NUMBER,
    PRODUCAO NUMBER
);
CREATE TABLE TB_POLUICAO_AGUA_CIDADES (
    ID NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY PRIMARY KEY,
    CIDADE VARCHAR2(100),
    REGIAO VARCHAR2(100),
    ENTIDADE VARCHAR2(100),
    QUALIDADE_AR NUMBER,
    POLUICAO NUMBER
);

SELECT * FROM TB_PRODUCAO_PLASTICO;
SELECT * FROM TB_POLUICAO_AGUA_CIDADES;

DELETE FROM TB_PRODUCAO_PLASTICO;
DELETE FROM TB_POLUICAO_AGUA_CIDADES;